worker_processes  1;
events {
   worker_connections 1024;
}

http {
    upstream servers {
         server <%=@server_ip1%>;
         server <%=@server_ip2%>;
    }

    server {
        listen <%=@balancer_port%>;

        location / {
              proxy_pass http://servers;
        }
    }
}
